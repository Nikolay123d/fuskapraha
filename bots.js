window.PF_BOTS=(function(){let db,auth;let timers={};function startForAdmin(fb){db=fb.database();auth=fb.auth();db.ref('settings/bots').on('value',s=>{Object.values(timers).forEach(clearInterval);timers={};const bots=s.val()||{};Object.keys(bots).forEach(id=>{const b=bots[id];if(!b||!b.interval||b.interval<5)return;timers[id]=setInterval(async()=>{const u=auth.currentUser;if(!u||!u.email)return;const adminEmail=(await db.ref('settings/adminEmail').get()).val();if(u.email!==adminEmail)return;await db.ref('messages/'+(b.city||'praha')).push({uid:u.uid,nick:u.displayName||'Бот',ava:u.photoURL||'public/images/ava.png',txt:b.text||'',ts:Date.now(),bot:true});},b.interval*1000);});});}return{startForAdmin};})();